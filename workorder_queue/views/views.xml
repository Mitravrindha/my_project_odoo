<odoo>
  <data>
<!--
   <record model="ir.ui.view" id="workorder_queue_list">
      <field name="name">order.queue.list</field>
      <field name="model">workorder.queue</field>
      <field name="arch" type="xml">
        <tree>
          <field name="machine_name"/>
          <field name="delivery_date"/>
          <field name="materials_recieved"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="workorder_queue_form">
      <field name="name">order.queue.form</field>
      <field name="model">workorder.queue</field>
      <field name="arch" type="xml">
        <form>
          <group>
            <group>
              <field name="machine_name"/>
              <field name="delivery_date"/>
              <field name="materials_recieved"/>
            </group>
          </group>
        </form>
      </field>
    </record>

    <record model="ir.actions.act_window" id="workorder_action_window">
      <field name="name">Work Order Queue</field>
      <field name="res_model">workorder.queue</field>
      <field name="view_mode">tree,form</field>
    </record>

        <menuitem name="Work Order Queue" id="workorder_menu" parent="sale.sale_order_menu"
              action="sale.action_orders"/>
-->
  </data>
</odoo>